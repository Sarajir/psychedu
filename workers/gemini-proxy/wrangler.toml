name = "psychedu-gemini-proxy"
main = "src/index.ts"
compatibility_date = "2025-04-01"

# Comma-separated origins allowed to call this worker (browser Origin header).
# Add your GitHub Pages origin and any custom domains.
[vars]
ALLOWED_ORIGINS = "http://localhost:5173,http://127.0.0.1:5173,http://localhost:4173,https://sarajir.github.io"
